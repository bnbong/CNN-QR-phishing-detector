# 실험 결과 — QR 모듈 격자 기반 피싱 URL 분류

설계 스펙은 `docs/EXPERIMENT_DESIGN.md`. 이 문서의 모든 표는 `reports/`의 집계 CSV·JSON에서
`scripts/make_results_tables.py`로 생성했다. 수치를 고칠 일이 있으면 CSV를 고치고 스크립트를 다시 돌린다.

---

## 1. 실험 요약

- **데이터**: WebPhish(Opara et al., 2024). URL 정규화(소문자화, 선행 `www.` 1회 제거, 말미 `/` 제거)
  후 중복·라벨 충돌 제거. QR 생성 조건은 EC=L, `optimize=0`(단일 8비트 바이트 청크), `mask_pattern=0` 고정.
  모델 입력은 PNG가 아니라 `(2, n, n)` 비트 격자(값 채널 + 데이터 모듈 마스크)다.
- **층**: QR 버전 v2/v3/v4가 primary(엄격 길이 매칭 후 클래스당 ≥1,000). v5+는 매칭 후 클래스당
  266개만 남아 사전 등록 규칙대로 **폐기**했다. 따라서 이 문서의 결론 범위는 URL 길이 18~78바이트 구간이다.
- **표본 수**(주 조건 `L-exact`): v2 12,118 / v3 9,178 / v4 2,714, 층 안에서 두 클래스 1:1.
- **분할과 통계**: eTLD+1 그룹 그리디 빈패킹 70/15/15, 단일 그룹 5% 상한 다운샘플. 시드 5개(0~4)를
  분할·서브샘플·초기화에 함께 걸어 평균과 SD를 낸다. 95% CI는 표본이 아니라 **그룹을 리샘플링하는
  클러스터 부트스트랩**이다.
- **누출 진단**: `reports/split_diagnostics.json`의 12개 층 셀 전부에서 split 간 eTLD+1 교집합이 0이고,
  test 상위 5개 그룹이 차지하는 비중은 18.7~20.6%다.
- **역매핑 게이트**: 통과(1,584건 검사, 실패 0). 모듈 ↔ 비트 ↔ 코드워드 ↔ URL 바이트 역매핑이
  라운드트립으로 검증되지 않으면 학습 자체를 돌리지 않는다.
- **상한선 부등식**: 모든 조건·층에서 CNN AUROC가 char n-gram LR 상한선 아래였다
  (`below_text_upper_bound` 전부 True). 상한선을 넘는 조건은 없었다. 즉 관측된 누출 신호는 없다.
- **하드웨어**: Colab T4 단일 GPU.

| 길이 통제 | 층 | 매칭 전 n | 매칭 후 n | benign | phishing | 등급 |
|---|---|---|---|---|---|---|
| exact | v2 | 18718 | 12118 | 6059 | 6059 | primary |
| exact | v3 | 11996 | 9178 | 4589 | 4589 | primary |
| exact | v4 | 3930 | 2714 | 1357 | 1357 | primary |
| exact | v5plus | 4147 | 532 | 266 | 266 | dropped |
| quantile | v2 | 18718 | 12200 | 6100 | 6100 | primary |
| quantile | v3 | 11996 | 9230 | 4615 | 4615 | primary |
| quantile | v4 | 3930 | 2802 | 1401 | 1401 | primary |
| quantile | v5plus | 4147 | 544 | 272 | 272 | dropped |
| none | v2 | 18718 | 16627 | 10489 | 6138 | primary |
| none | v3 | 11996 | 11996 | 7350 | 4646 | primary |
| none | v4 | 3930 | 3930 | 1404 | 2526 | primary |
| none | v5plus | 4147 | 4147 | 272 | 3875 | dropped |

---

## 2. 주 결과 (T3)

주 조건은 `norm` · `L-exact` · `data_only`(기능 패턴 제거) · `mask-fixed` · SmallCNN이다.
셀은 `AUROC 평균 [클러스터 부트스트랩 95% CI]`.

| 모델 / 기준선 | v2 | v3 | v4 |
|---|---|---|---|
| SmallCNN (주 조건) | 0.872 [0.833, 0.906] | 0.814 [0.751, 0.869] | 0.805 [0.744, 0.861] |
| BitMLP | 0.834 [0.786, 0.876] | 0.732 [0.672, 0.791] | 0.690 [0.615, 0.757] |
| 라벨 셔플 (바닥) | 0.498 [0.453, 0.541] | 0.508 [0.460, 0.554] | 0.506 [0.437, 0.576] |
| char n-gram LR (상한선) | 0.969 | 0.972 | 0.992 |
| byte-hist LR | 0.861 | 0.766 | 0.771 |
| length LR | 0.500 | 0.500 | 0.500 |
| version LR | 0.500 | 0.500 | 0.500 |
| 상한선 − CNN 갭 | +0.097 | +0.158 | +0.187 |

라벨 셔플이 세 층 모두 0.50 근방에 붙고 CI가 0.5를 포함하므로 파이프라인 자체의 누출은 없다.
length LR과 version LR은 `L-exact`에서 정확히 0.500인데, 이것은 성능이 아니라 **설계가 의도대로
작동했다는 확인**이다. 층 안에서 버전이 고정이고 길이 주변분포가 클래스 간 동일하므로 두 변수는
정의상 라벨에 대한 정보를 갖지 않는다.

---

## 3. H4 — 공간 구조 검정

`shuffle-pos`는 모든 표본에 **동일한 고정 순열**을 걸어 픽셀 값 집합은 그대로 두고 위치 정보만 파괴한다.
BitMLP는 순열 불변이므로 대조군 역할을 한다.

| 조건 | v2 | v3 | v4 |
|---|---|---|---|
| CNN · 정상 배치 | 0.872 [0.833, 0.906] | 0.814 [0.751, 0.869] | 0.805 [0.744, 0.861] |
| CNN · shuffle-pos | 0.765 [0.706, 0.818] | 0.671 [0.618, 0.723] | 0.656 [0.593, 0.719] |
| CNN 차이 | -0.107 | -0.143 | -0.149 |
| MLP · 정상 배치 | 0.834 [0.786, 0.876] | 0.732 [0.672, 0.791] | 0.690 [0.615, 0.757] |
| MLP · shuffle-pos | 0.834 [0.787, 0.876] | 0.733 [0.673, 0.793] | 0.706 [0.634, 0.770] |
| MLP 차이 | +0.000 | +0.001 | +0.016 |

---

## 4. 절제

### 4-1. 길이 통제 (H3)

| 조건 | v2 | v3 | v4 |
|---|---|---|---|
| L-exact (주 조건) | 0.872 [0.833, 0.906] | 0.814 [0.751, 0.869] | 0.805 [0.744, 0.861] |
| L-quantile | 0.853 [0.811, 0.891] | 0.817 [0.757, 0.870] | 0.797 [0.732, 0.857] |
| L-none | 0.877 [0.843, 0.904] | 0.816 [0.757, 0.870] | 0.829 [0.770, 0.881] |
| L-none − L-exact | +0.005 | +0.002 | +0.024 |
| PAD-rand (L-none 위) | 0.862 [0.824, 0.893] | 0.795 [0.735, 0.850] | 0.799 [0.734, 0.858] |
| PAD-rand − L-none | -0.015 | -0.021 | -0.030 |
| length LR @ L-exact | 0.500 | 0.500 | 0.500 |
| length LR @ L-none | 0.543 | 0.544 | 0.610 |
| version LR @ L-none | 0.500 | 0.500 | 0.500 |

### 4-2. 표현·마스크·부분집합

| 조건 | v2 | v3 | v4 |
|---|---|---|---|
| norm · mask-fixed · data_only (주 조건) | 0.872 [0.833, 0.906] | 0.814 [0.751, 0.869] | 0.805 [0.744, 0.861] |
| raw (정규화 해제) | 0.921 [0.896, 0.943] | 0.873 [0.809, 0.928] | 0.892 [0.846, 0.930] |
| raw − norm | +0.049 | +0.060 | +0.087 |
| feat-all (기능 패턴 포함) | 0.873 [0.834, 0.905] | 0.802 [0.743, 0.857] | 0.813 [0.753, 0.866] |
| feat-all − data_only | +0.001 | -0.012 | +0.008 |
| mask-auto | 0.748 [0.709, 0.785] | 0.678 [0.634, 0.722] | 0.675 [0.615, 0.733] |
| mask-auto − mask-fixed | -0.124 | -0.135 | -0.130 |
| maskindex LR @ mask-auto | 0.524 | 0.486 | 0.517 |
| mask-off (언마스킹) | 0.871 [0.829, 0.907] | 0.803 [0.736, 0.865] | 0.816 [0.755, 0.868] |
| mask-off − mask-fixed | -0.001 | -0.011 | +0.011 |
| path+ 부분집합 | 0.727 [0.645, 0.805] | 0.766 [0.696, 0.836] | 0.822 [0.759, 0.877] |
| path+ 상한선 (char n-gram LR) | 0.914 | 0.968 | 0.988 |
| EC-M (v3 한정) | — | 0.847 [0.797, 0.883] | — |
| EC-M 상한선 (char n-gram LR) | — | 0.961 | — |

---

## 5. Grad-CAM (P3)

해석 대상 조건은 `norm-exact-data_only-off-small_cnn`(마스크 해제, 언마스킹 격자)이고 층당 128표본이다.
언마스킹 격자를 쓰는 이유는 마스크 XOR이 걷힌 상태라야 CAM 질량을 코드워드 종류(kind)에 곧바로 귀속시킬 수 있어서다.

| CAM 질량 (kind) | v2 | v3 | v4 |
|---|---|---|---|
| char (URL 문자) | 38.6% | 54.9% | 60.7% |
| pad (패딩) | 9.4% | 8.7% | 11.6% |
| ec (오류정정) | 13.7% | 8.1% | 10.2% |
| function (기능 패턴) | 33.8% | 26.7% | 16.0% |
| length_header | 1.5% | 1.1% | 0.9% |
| mode_header | 0.8% | 0.4% | 0.3% |
| remainder | 0.7% | 0.1% | 0.3% |
| char / 데이터 모듈 내 비율 | 60.3% | 75.1% | 72.5% |
| n_samples | 128 | 128 | 128 |

`char_position_curve`는 URL 상대 위치를 50구간으로 나눠 각 구간에 실린 CAM 질량을 클래스별로 집계한 곡선이다(원본 값은 `reports/explain_summary.json`).

- 세 층 모두 phishing 곡선이 전 구간에서 benign 곡선보다 위에 있다. 구간 평균은 v2 0.362 대 0.125,
  v3 0.427 대 0.298, v4 0.325 대 0.172다.
- 모양은 앞쪽 우세다. v3·v4는 URL 앞 10% 부근에서 최대(v3 phishing 0.587, benign 0.451)를 찍고 뒤로 갈수록
  단조에 가깝게 떨어진다. v2 phishing만 40% 지점 부근(0.480)에 봉우리가 있다.
- 앞쪽 우세는 스킴·호스트 구간에 해당한다. 뒤쪽 감소는 해당 구간이 패딩에 가까워지면서 문자 모듈 자체가
  줄어드는 효과와 섞여 있다. 곡선의 절대 높이보다 클래스 간 상대 격차를 읽는 쪽이 안전하다.

그림(F6 평균 히트맵, F7 스택 막대, F8 위치 곡선)은 다음 작업에서 생성한다.

---

## 6. 해석

### 6-1. H1·H2 — 디코딩 없이 어휘 신호의 상당 부분이 복원된다

길이·버전·패딩 경계를 모두 통제한 주 조건에서 CNN은 v2 0.872, v3 0.814, v4 0.805를 냈다. 같은 층의
라벨 셔플 바닥은 0.498 ~ 0.508, 텍스트 상한선(char n-gram LR)은 0.969~0.992다. 갭은 0.097 ~ 0.187로,
바닥에서 상한선까지의 거리 중 적잖은 부분을 CNN이 메운 셈이다. 디코딩 없이 QR 모듈 배치만 보고도 URL
어휘 신호의 큰 몫을 복원할 수 있다는 것, 이것이 이 실험의 1차 결론이다.

갭이 층이 올라갈수록 벌어진다는 점(0.097 → 0.158 → 0.187)도 기록해 둔다. URL이 길수록 격자에 담기는
문자 수는 늘지만 CNN이 그것을 읽어내는 난도가 함께 올라간다. v4는 표본이 2,714개로 가장 작아 학습량
부족이 섞여 있을 가능성도 배제할 수 없다.

### 6-2. H4 — 성능은 공간·순서 구조에서 온다 (핵심 결과)

고정 순열로 위치만 흩으면 CNN은 0.872 → 0.765, 0.814 → 0.671, 0.805 → 0.656으로 무너진다(−0.107,
−0.143, −0.149). 같은 순열을 먹인 BitMLP는 0.834 → 0.834, 0.732 → 0.733, 0.690 → 0.706으로 사실상
움직이지 않는다. 순열 불변 모델이 안 움직이고 합성곱 모델만 떨어진다. 이 하락은 데이터 파괴가 아니라
공간 구조를 쓰던 모델이 그 구조를 잃은 결과다. 논문의 핵심 그림 F5는 이 2×2다.

MLP의 v4만 +0.016으로 미세하게 올랐는데, 표본이 가장 작은 층의 시드 변동(SD 0.018~0.019) 범위 안이라
방향성 있는 효과로 읽지 않는다.

### 6-3. CNN이 순서 없는 바이트 분포 이상을 본다

층별로 CNN 0.872/0.814/0.805, BitMLP 0.834/0.732/0.690, byte-hist LR 0.861/0.766/0.771이다. 순서를
완전히 버린 바이트 히스토그램 LR만으로 BitMLP와 같은 수준이 나온다. v2·v4에서는 오히려 더 높다.
위치 정보를 쓰지 못하는 모델의 성능은 바이트 주변분포로 거의 설명된다. 그 위에 CNN이 얹는
추가 이득이 6-2의 공간·순서 구조에서 오는 몫이다.

### 6-4. H3 — 길이 아티팩트는 작다

`L-none`과 `L-exact`의 차이는 +0.005 / +0.002 / +0.024로 작고, v2·v3는 CI 폭(0.06~0.12) 안에서
구분되지 않는다. 패딩 주기 패턴을 직접 깨는 `PAD-rand`도 `L-none` 대비 −0.015 / −0.021 / −0.030에 그친다.
길이 단독 LR은 통제를 완전히 푼 `L-none`에서조차 0.543 / 0.544 / 0.610이고, version LR은 층 안에서
버전이 고정이므로 정의상 0.500이다.

해석은 이렇다. 버전 층화와 URL 정규화가 이전 실험의 누출 대부분을 이미 제거했고, 그 위에 얹은 엄격
길이 매칭이 추가로 걷어내는 몫은 작다. 결론의 방향은 "길이 통제를 해도 성능이 유지된다"이지 "길이가
성능의 원천이었다"가 아니다. 다만 v4는 차이가 0.024로 다른 두 층보다 크다. `L-none`의 v4는 클래스 비가
1,404 대 2,526으로 크게 기울어 있어(표 T2) 이 층에 한해 잔여 아티팩트를 완전히 배제하지는 않는다.

### 6-5. 정규화가 성능의 5~9%p를 좌우한다

`raw`(정규화 해제)는 `norm`보다 +0.049 / +0.060 / +0.087 높다. 선행 `www.`와 대소문자만으로 성능이
5~9%p 움직인다. 이전 실험이 보고한 87%대 수치의 출처가 적잖이 여기에 있다. 두 조건은
각각 독립적으로 dedup되어 표본 구성이 다르므로(raw v3는 11,594, norm v3는 9,178) 절대값 비교보다
"정규화가 손대는 표면 신호의 크기"로 읽어야 한다. 주 결과를 `norm`으로 잡은 이유가 이것이다.

### 6-6. 마스크

mask-auto는 −0.124 / −0.135 / −0.130으로 크게 떨어진다. 반면 mask-off는 mask-fixed와 사실상 동일하고
(−0.001 / −0.011 / +0.011), maskindex LR은 0.524 / 0.486 / 0.517로 우연 수준이다.

세 결과를 합치면 원인이 특정된다. 마스크 인덱스 자체는 라벨의 프록시가 아니다. 문제는 자동 마스크 선택이
같은 URL을 8가지 XOR 패턴 중 하나로 보낸다는 데 있다. 동일한 데이터가 8개의 서로 다른 시각 사상으로
갈라지고, 그만큼 학습이 어려워진다. 마스크를 아예 벗겨도 고정 마스크와 성능이 같다는 사실이 이를 뒷받침한다.
CNN이 배우는 것은 마스크 패턴이 아니라 그 아래 코드워드 배치다.

### 6-7. 기능 패턴, path+, EC-M

- **feat-all vs data_only**: +0.001 / −0.012 / +0.008. 차이 없다. 기능 패턴(파인더·타이밍·정렬)은 층 안에서
  상수 입력이므로 예상대로 정보를 더하지 않는다.
- **path+**: v2에서 0.727로 뚜렷이 낮아지지만(주 조건 0.872) 라벨 셔플 바닥보다는 한참 위다. v3 0.766,
  v4 0.822로 층이 올라갈수록 회복한다. 이 부분집합은 상한선 자체도 v2에서 0.914로 낮아지므로
  (주 조건 0.969) 난도가 올라간 문제를 풀고 있는 것이지 신호가 사라진 것이 아니다.
- **EC-M**: v3에서 0.847. 다만 EC 레벨을 바꾸면 같은 URL이 다른 용량·다른 층으로 이동해 표본 구성이
  달라진다(n 10,482 대 9,178, 상한선 0.961 대 0.972). 주 결과와 직접 비교할 수 없고 강건성 확인으로만
  쓴다.

### 6-8. Grad-CAM

char 영역 CAM 질량은 층이 올라갈수록 38.6% → 54.9% → 60.7%로 증가하고, pad 영역은 8.7~11.6%에 머문다.
버전이 커질수록 데이터 모듈 중 문자 비중이 늘어나는 QR 구조와 방향이 맞는다.

function 영역 질량(33.8% → 26.7% → 16.0%)은 액면 그대로 읽으면 안 된다. 기능 패턴은 층 안에서 모든 표본에
동일한 상수 입력이고 6-7에서 보듯 성능에 기여하지 않는다. 이 질량은 마지막 합성곱 특징맵을 원래 격자
크기로 업샘플링할 때 생기는 번짐이 기능 패턴 영역으로 흘러든 것으로 본다. 격자가 작을수록 번짐 비중이
커지는 층별 추세(v2 > v3 > v4)도 이 해석과 일치한다. 보고할 때는 데이터 모듈 안에서 차지하는 상대 비율을
쓰는 편이 낫다. 그 기준으로 char 비중은 60.3% / 75.1% / 72.5%다.

---

## 7. 한계

1. **benign이 Alexa 계열 상위 사이트에 치우쳐 있다.** WebPhish의 ham은 인기 도메인 목록에서 왔고
   phishing은 신고 기반이다. "피싱 대 정상"이 아니라 "신고된 URL 대 인기 URL"을 풀고 있을 여지가 남는다.
   6-5의 정규화 민감도(선행 `www.`·대소문자로 5~9%p)가 이 편향의 한 단면이다.
2. **v5+ 폐기로 주장 범위가 좁다.** 결론은 URL 18~78바이트, QR v2 ~ v4 구간에 한정된다. 긴 URL 구간은
   엄격 길이 매칭 후 클래스당 266개만 남아 검정력이 없다.
3. **단일 데이터셋.** 데이터셋 고유의 수집 편향과 QR 신호를 분리할 외부 검증이 없다.
4. **`L-none` v4의 클래스 불균형**(1,404 대 2,526)이 6-4의 잔여 해석을 제한한다.
5. **`PAD-rand`는 비규격 QR**이다. 인과 증거로는 가장 강하지만 설계 결정대로 부록에 둔다.
6. **2단계(PhishTank/Tranco 신규 수집)는 인터페이스만 정의된 상태**로 실행하지 않았다.

## 8. 다음 단계

1. 그림 생성: F2(길이 분포 매칭 전후), F3(`first_pad_codeword` 분포), F4(층별 AUROC + CI에 상한선·바닥
   수평선), **F5(shuffle-pos 2×2)**, F6~F8(Grad-CAM 히트맵·kind 스택 막대·위치 곡선).
2. 주 가설 4개(H1 ~ H4)에 대한 Holm 보정 결과를 표로 명시.
3. 논문 초안: 상한선/하한선/본실험 3층 구조로 서술하고, F5를 핵심 그림으로 배치.
4. 2단계 데이터 수집 여부 결정(리뷰 대응상 Tranco benign 추가 검토).
